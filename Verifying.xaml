<Window x:Class="WPFDemo.Verifying"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFDemo"
        mc:Ignorable="d"
        Title="Verifying" Height="150" Width="350"
         AllowsTransparency="True" Background="Transparent"
        WindowStyle="None" ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen" 
        Loaded="Window_Loaded">

    <Border BorderThickness="1,1,1,1" CornerRadius="10" >

        <Border.Background >
            
            <LinearGradientBrush Opacity="0.9">
                <GradientStop Color="#cfe9ff" Offset="0.0"/>
                <GradientStop Color="#4287f5" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>

        <Grid>
            <TextBlock 
                HorizontalAlignment="Center" 
                VerticalAlignment="Center"
                Text="Opening Window..." 
                FontFamily="Corbel Light" 
                FontSize="30" 
                FontWeight="ExtraBold"
                Foreground="White"/>

        </Grid>
        </Border>
</Window>
